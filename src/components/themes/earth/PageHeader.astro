---
interface Props {
  name: string;
  bio: string;
}

const { name, bio } = Astro.props;
---

<header class="mb-12">
  <h1 class="text-4xl font-bold text-foreground mb-4">{name}</h1>
  <p class="text-lg text-muted-foreground leading-relaxed">{bio}</p>
</header>
